[package]
name = "wallet-service"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Validated chain-neutral domain types and wallet abstractions for crypto deposits"
license = "MIT"

[dependencies]
num-bigint = "0.4"
thiserror = "2"

[lints.rust]
missing_docs = "deny"
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
